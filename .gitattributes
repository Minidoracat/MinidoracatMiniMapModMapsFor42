# 伺服器端執行的 shell script 必須 LF（CRLF 會讓 heredoc 資料列尾欄帶 CR、算術直接炸）
*.sh text eol=lf

# PZ Lua/JSON 對行尾敏感，統一 LF（CRLF 會讓翻譯檔被引擎靜默忽略）
*.json text eol=lf
*.lua text eol=lf
*.txt text eol=lf
*.py text eol=lf
*.ps1 text eol=lf
*.bat text eol=lf
*.md text eol=lf
*.yml text eol=lf
mod.info text eol=lf
.gitattributes text eol=lf

# workshop.txt 例外：遊戲上傳會整檔回寫成 CRLF，跟著遊戲格式走以消除假差異（入庫仍 LF）
MOD/*/workshop.txt text eol=crlf

# 街名替代檔是**生成物**，契約是「逐字節等於上游套用確定性轉換的結果」，而行尾跟隨
# 各自上游（實測 clover-lake／daisy-county 是 LF、anruisi-town／muldraugh-1993 是 CRLF）。
# 一旦讓 git 做行尾轉換，checkout 會改變檔案內容 ⇒
# `python scripts/gen_streets_i18n.py verify` 的逐字節比對直接 fail（CI 與新 clone 都會炸）。
# 故明確關閉轉換（`-text`），逐字節原樣進出。
MOD/*/Contents/mods/*/42/media/minimapstreets/**/*.xml -text
